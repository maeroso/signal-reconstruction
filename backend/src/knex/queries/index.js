const { products, productDetail } =  require('./product')
const { application, userRoleApplication } =  require('./application')
const { companyMenu } =  require('./menu')
const { table } =  require('./table')

module.exports = {
    productDetail,
    products,
    application,
    userRoleApplication,
    companyMenu,
    table,
}